{
    "configuration": {
        "dev": {
            "useSsh": true,
            // Please set these secrets and salt before development
            // cd WebApi
            // dotnet user-secrets set "Ssh:Username" "root" 
            // dotnet user-secrets set "Ssh:Password" "<rootPass>" 
            // dotnet user-secrets set "Ssh:Host" "<ip>" 
            "privateKey": "Minimum20SizeLengthKey",
            "salt": "MySuperSalt", // Change to the same salt, that you put in installer
            "sqlliteDbName": "armnas.db"
        },
        "prd": {
            "useSsh": false,
            "privateKey": "<KEY_GENERATED_BY_INSTALLER>",
            "salt": "<SALT_INSERTED_BY_INSTALLER>",
            "sqlliteDbName": "armnas.db"
        }
    }
}